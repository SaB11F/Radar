node_modules
porocilo/

# Device secrets — contains radarId + deviceKey, never commit
device/config/uplink.json

# Claude Code local settings (machine-specific)
.claude/

# Device runtime artifacts (created while the pipeline runs)
device/data/
device/src/__pycache__/
__pycache__/
*.pyc
