Place OpenCV DNN model files here for optional vehicle recognition:

- `MobileNetSSD_deploy.prototxt`
- `MobileNetSSD_deploy.caffemodel`

If these files are missing, `radar_device.py` automatically falls back to background-subtractor detection.
